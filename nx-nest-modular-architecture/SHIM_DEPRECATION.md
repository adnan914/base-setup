# Shim Deprecation Checklist

Completed:

1. Legacy shim imports were removed
2. Feature-library paths are now primary
3. Old path aliases were removed from `tsconfig.base.json`
4. Compatibility shim libraries were deleted
5. Legacy monolith `apps/api` was removed from the workspace
