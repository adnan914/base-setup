export enum TokenType {
  ACCESS = "ACCESS",
  REFRESH = "REFRESH",
  FORGOTPASSWORD = "FORGOTPASSWORD"
}

export enum JoiValidateType {
  BODY = "BODY",
  QUERY = "QUERY",
  PARAM = "PARAM"
}

export enum Status {
  ACTIVE = "Active",
  INACTIVE = "Inactive",
}